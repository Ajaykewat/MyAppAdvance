import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Feed } from '@lens-protocol/react-native-lens-ui-kit'

const LensUIKet = () => {
  return (
    <View>
      <Text>LensUIKet</Text>
      <Feed />
    </View>
  )
}

export default LensUIKet

const styles = StyleSheet.create({})